# Server
PORT=3000
NODE_ENV=development

# Database (Sequelize — wire when ready)
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=
# DB_NAME=hospital_reviews

# Auth (JWT — wire when ready)
# JWT_SECRET=
# JWT_EXPIRES_IN=1d
